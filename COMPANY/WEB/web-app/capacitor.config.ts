import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cleanwavenigltd.app',
  appName: 'Cleanwave Recycling',
  webDir: 'dist'
};

export default config;
